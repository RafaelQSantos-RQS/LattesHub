interface:
  display_name: "GitHub PR Creator"
  short_description: "Create polished PRs with GitHub CLI metadata"
  default_prompt: "Use $gh-create-pr to create a pull request for the current code changes with the right GitHub metadata."
